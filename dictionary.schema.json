{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "array",
    "minItems": 1,
    "items": {
        "type": "object",
        "required": [
            "name",
            "word",
            "definition"
        ],
        "properties": {
            "name": {
                "type": "string",
                "description": "Required. Unique ID of the dictionary entry."
            },
            "word": {
                "type": "string",
                "description": "Required. The word of the entry."
            },
            "definition": {
                "type": "string",
                "description": "Required. The definition of the word."
            },
            "image": {
                "type": "string",
                "description": "Optional. Name of the image file in the media folder. Eg. `tax.png`."
            }
        }
    }
}